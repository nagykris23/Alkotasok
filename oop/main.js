const elvalaszto = document.createElement('hr');// létrehoz egy hr elemet   
document.body.appendChild(elvalaszto);// hozzáadja a bodyhoz
const table = new Area('table');// létrehoz egy Area objektumot a table osztállyal
const form = new Area('form');// létrehoz egy Area objektumot a form osztállyal